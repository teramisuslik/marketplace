spring:
  application:
  name: serviceForCast
  datasource:
    url: jdbc:postgresql://localhost:5432/Cast
    username: postgres
    password: 210009
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
server:
  port: 8083