name: Build serviceForProduct

on:
  push:
    paths:
      - 'serviceForProduct/**'
      - '.github/workflows/build-serviceforproduct.yml'
  pull_request:
    paths:
      - 'serviceForProduct/**'
      - '.github/workflows/build-serviceforproduct.yml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
      - name: Build
        run: |
          cd serviceForProduct
          mvn clean verify